# Frontend feature flag'leri — kopyala: cp .env.example .env.local
# Değişiklik sonrası dev sunucusunu / build'i yeniden başlatın.

# Ara gün seçimi (rezervasyonda haftanın tek tek günlerini seçme).
# Varsayılan kapalı: yalnız toplu tarih aralığı seçilir.
# VITE_FEATURE_WEEKDAY_SELECTION=true
